EJERCICIO 1.1
int a = 150, j=450;
void setup(){
  size(600,600);
  background(255);
}

void draw() {
  ellipseMode(CENTER);
  //for(int a =150; j<=300;a++){
     //for(int j =150; j<=300;j--){
       if(a<=300 && j >=300){
     background(255);    
    ellipse(a,a,100,100);
    ellipse(j,a,100,100);
    ellipse(a,j,100,100);
    ellipse(j,j,100,100);
    a++;
    j--;
       }
     }
     
